info:
  name: list_batches
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/batches"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
