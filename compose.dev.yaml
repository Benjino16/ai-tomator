services:
  backend:
    build: .
    environment:
      - SECURE_COOKIES=false

  worker:
    build: .
    command: celery -A ai_tomator.celery.worker worker --loglevel=debug -E --concurrency=2

  beat:
    build: .

  frontend:
    build: ./frontend